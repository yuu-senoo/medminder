{
  "name": "medminder-remind-cron",
  "version": "1.0.0",
  "private": true,
  "description": "服薬リマインド用 Cloudflare Worker (Cron Trigger)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "wrangler": "^4.20.0"
  }
}
